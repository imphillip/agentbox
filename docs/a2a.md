# No AgentBox A2A service was released

An A2A mailbox registration path appeared in product plans and interface placeholders, but it was not implemented before the `agentbox.id` mailbox experiment ended.

Read [the product evolution retrospective](./background/from-mailbox-to-agent-first) for the distinction between the agent-first interface and the control plane that actually shipped.
