# Mailbox configuration has ended

The `agentbox.id` mailbox service is no longer operating. Existing configuration and credential links can no longer be used.

Read [the mailbox experiment retrospective](./mailbox) or return to [the agentbox archive](./).
