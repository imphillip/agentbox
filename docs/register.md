# Registration has ended

The `agentbox.id` mailbox service is no longer operating and is not accepting registrations.

Read [the mailbox experiment retrospective](./mailbox) or return to [the agentbox archive](./).
