# plugins

No agentbox plugins were released.

This directory was created for runtime integrations that might have connected agent frameworks to the experimental mailbox service. The service closed before that plugin model was validated or implemented.

The placeholder is retained only so old links and the repository's original layout remain understandable. It does not represent a roadmap.
